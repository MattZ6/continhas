import { styled } from '@styles/stitches.config';

export namespace DateFormFieldStyles {
  export const Container = styled('div', {});

  export const Field = styled('label', {});

  export const Picker = styled('div', {});
}
