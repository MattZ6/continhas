import { styled } from '@styles/stitches.config'

export namespace HeaderStyles {
  export const Container = styled('header', {
    padding: '$normal',
    borderBottom: '1px solid $defaultSubtleBorder',
  })
}
